management:
  endpoint:
    metrics:
      enabled: true
  tracing:
    enabled: true
    sampling.probability: 1.0
    propagation:
      type: b3
    baggage:
      correlation:
        fields:
          - SomeHeader
      remote-fields:
        - SomeHeader
